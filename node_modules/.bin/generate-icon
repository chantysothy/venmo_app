#!/bin/sh
basedir=`dirname "$0"`

case `uname` in
    *CYGWIN*) basedir=`cygpath -w "$basedir"`;;
esac

if [ -x "$basedir/node" ]; then
  "$basedir/node"  "$basedir/../react-native-vector-icons/generate-icon.js" "$@"
  ret=$?
else 
  node  "$basedir/../react-native-vector-icons/generate-icon.js" "$@"
  ret=$?
fi
exit $ret
